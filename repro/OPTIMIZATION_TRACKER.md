# Optimization / Refactor Tracker — DISC multivariate exDQLM (Wishart, ensembles)

Scope: **only** the multivariate exDQLM ensemble workflow anchored at:

- Entrypoint: `DISC_Optimal_Synth_Ranges_W.r`
- Direct compiled deps: `sampling_exal.cpp`, `sampling_truncnorm.cpp`, `DISC_kalman_synth.cpp`

Hard exclusions (never edit):
- `DISC_Optimal_Synth_Ranges_NDLM.r`
- `DISC_Optimal_Synth_Ranges.r`

This tracker is stage-by-stage. Each stage ends with:
1) a validation run, 2) an equivalence check, 3) a tracker update.

---

## Canonical run commands

### Single run (entrypoint wrapper; deterministic seed)

```bash
cd /data/muscat_data/jaguir26/project1_ucsc_phd
Rscript --vanilla scripts/run_DISC_Optimal_Synth_Ranges_W.R 0.5 777
```

Notes:
- The wrapper sets `set.seed(777)` and `RNGkind("Mersenne-Twister","Inversion","Rejection")`.
- It forces single-threaded math by default (`OMP_NUM_THREADS=1`, `OPENBLAS_NUM_THREADS=1`, `MKL_NUM_THREADS=1`).

### Stage 0 reproducibility check (two-run baseline)

```bash
cd /data/muscat_data/jaguir26/project1_ucsc_phd
# Restore locked initial state (required for equivalence vs the locked hash):
cp --reflink=auto repro/baseline_runs/20260204_174008_p0_0.5_seed_777/inputs/DISC_variables_50_exAL_synth_DISC.RData \
  DISC_variables_50_exAL_synth_DISC.RData
bash repro/run_stage0_baseline.sh 0.5 777
```

This:
- runs the workflow **twice** from an identical starting state,
- records logs + `sessionInfo()`,
- hashes key inputs/outputs,
- and asserts the key output hash is identical across runs.

---

## Inputs / outputs (static inventory; Wishart workflow)

### External inputs (absolute paths referenced by `DISC_Optimal_Synth_Ranges_W.r`)
- `/data/muscat_data/jaguir26/projects/Project/Input/exAL/parameters/parameters.txt`
- `/data/muscat_data/jaguir26/projects/Project/Input/exAL/covariates/cov_1_ELI.csv`
- `/data/muscat_data/jaguir26/projects/Project/Input/exAL/covariates/cov_2_ONI.csv`

### Repo-local inputs (absolute in script, but within this repo)
- `nws_forecast.csv`
- `weighted_time_series.csv`
- `prism_precipitation_santa_cruz_1987_2023.csv`
- `soil_moisture_data/soil_moisture_big_trees_daily_avg_1987_2023.csv`
- `pca.csv`
- `retros_2022-12-25.csv`
- Initial VB state (depends on `p0`): `DISC_variables_50_exAL_synth_DISC.RData` (for `p0=0.5`)

### Primary output(s)
- Updated VB state written back into:
  - `DISC_variables_<p0*100>_exAL_synth_DISC.RData` (e.g. `DISC_variables_50_exAL_synth_DISC.RData`)

---

## Golden baseline (Stage 0)

- Baseline capture root: `repro/baseline_runs/`
- Layout for a run (auto-generated by `repro/run_stage0_baseline.sh`):
  - `repro/baseline_runs/<ts>_p0_<p0>_seed_<seed>/`
    - `inputs/inputs.sha256`
    - `inputs/DISC_variables_<...>.RData` (backup for repeatability)
    - `meta/command_and_env.txt`
    - `meta/git_status.txt`
    - `meta/git_log.txt`
    - `meta/outputs_diff.txt`
    - `meta/console_diff.txt`
    - `run1/console.txt`, `run1/sessionInfo.txt`, `run1/outputs.sha256`
    - `run2/console.txt`, `run2/sessionInfo.txt`, `run2/outputs.sha256`

Golden hashes:
- Recorded per-baseline directory in `run1/outputs.sha256` and verified identical to `run2/outputs.sha256`.

Latest locked baseline (p0=0.5, seed=777):
- `repro/baseline_runs/20260204_174008_p0_0.5_seed_777/`
- Primary output hash (identical across run1/run2): `88dd2101b08f452b054ca191965802ce4b24d09bab407970c9f32d3657cdd56c` for `DISC_variables_50_exAL_synth_DISC.RData`
Notes:
- `console.txt` differs between run1/run2 due to wall-clock timing messages; we treat it as provenance, not an equivalence target.

---

## Stage checklist

| Stage | Goal | Status | Validation | Equivalence method |
|---:|---|---|---|---|
| 0 | Baseline + reproducibility lock-in | DONE | `bash repro/run_stage0_baseline.sh 0.5 777` | SHA256 of primary output `.RData` |
| 1 | Modularize helpers (no semantic change) | IN PROGRESS | rerun Stage 0 command | output SHA256 must equal locked hash |
| 2 | Normalize ensemble representation | TODO | rerun Stage 0 command | compare baseline `outputs.sha256` |
| 3 | Documentation | TODO | N/A | N/A |
| 4 | Safe performance optimization | TODO | rerun Stage 0 command | compare baseline `outputs.sha256` + timing log |

### Stage 1 sub-stages

| Substage | Goal | Status | Commit | Validation | Result |
|---:|---|---|---|---|---|
| 1.1 | Scaffolding (`R/disc_w/`, `DISC_DEBUG`, `disc_assert`) | PASS | `aa8f9cd` | `bash repro/run_stage0_baseline.sh 0.5 777` | output SHA256 = `88dd2101…` |
| 1.2 | Paths + input inventory helper | PASS | `d1768e0` | `bash repro/run_stage0_baseline.sh 0.5 777` | output SHA256 = `88dd2101…` |
| 1.3 | Loader helpers (I/O only) | PASS | (pending commit) | `bash repro/run_stage0_baseline.sh 0.5 777` | output SHA256 = `88dd2101…` |
| 1.4 | Covariate construction + standardization helpers | TODO |  |  |  |
| 1.5 | Ensemble bookkeeping helpers | TODO |  |  |  |
| 1.6 | Save-state / naming helper | TODO |  |  |  |

---

## Decisions log

- **Stage 0 determinism**: use wrapper `scripts/run_DISC_Optimal_Synth_Ranges_W.R` to set seed + single-thread env vars *without editing* `DISC_Optimal_Synth_Ranges_W.r`.
- **Repeatability**: `repro/run_stage0_baseline.sh` restores the mutable input/output `.RData` between run 1 and run 2 so both runs start from identical state.
- **Bugfix to enable Stage 0**: avoided clobbering the mathematical constant `pi` during sampling (renamed a local variable used for the logistic transform), which previously broke `update_sts` entropy calculations in SIMS mode.
- **Thread-safety**: forced `sample_gig_devroye_vector(...)` to run single-threaded because it uses `R::runif` internally.
- **Equivalence protocol**: always restore the locked Stage 0 initial `.RData` from `repro/baseline_runs/20260204_174008_p0_0.5_seed_777/inputs/` before running the baseline check, because the workflow updates the `.RData` state in-place.

---

## Performance log

(Populate after Stage 0 is locked in and Stage 4 begins.)
