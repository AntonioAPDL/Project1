# Theory ↔ Code Mapping Table (Static)

This table maps each theory contract item (Ti) from `theory_spec_checklist.md` to the *actually-used* code path
for `DISC_Optimal_Synth_Ranges_W.r` (see `audit_used_code.md` for the used-entrypoint inventory).

Columns:
- **LaTeX reference**: equation labels and/or `main.tex` line anchors.
- **Code reference**: file + function + line range.
- **Notes**: how the mapping is realized (e.g., offsets, stacking).

| Ti | LaTeX reference | Code location (file:function:line range) | Notes |
|---|---|---|---|
| T1 | `main.tex:60–110` (`eq:A_obs`, `eq:B_obs`) | `DISC_Optimal_Synth_Ranges_W.r:960–970` (construct `GG`, `FF`); `DISC_kalman_synth.cpp:791–905` (`DISC_update_theta_synth_cpp_W`) | R builds a block-structured measurement matrix: baseline loadings shared across all obs columns + per-product discrepancy blocks (matches “baseline + discrepancy” structure). C++ uses `f = FF.slice(t).t() * a + ex_f.col(t)`. |
| T2 | `main.tex:101–111` (`eq:B_delta`) | `DISC_Optimal_Synth_Ranges_W.r:960–965` (block-diag `GG`); `DISC_Optimal_Synth_Ranges_W.r:246–257` (`make_df_mat` discount-factor matrix); `DISC_kalman_synth.cpp:883–901` (`DISC_update_theta_synth_cpp_W`) | Code evolves each block with the same `GG` and implements \(W_t\) for historical filtering via discount factors: `R = P % ex_df_mat + P % Ones` so effectively `R = P + (P ∘ ex_df_mat)`. |
| T3 | `main.tex:113–159` (`eq:C_obs`, `eq:C_state`, `eq:C_IW`) | `DISC_Optimal_Synth_Ranges_W.r:1015–1026` (build nested `GG_list`, `FF_list`); `DISC_Optimal_Synth_Ranges_W.r:1492–1494` (build `ensembles_forecast`); `DISC_Optimal_Synth_Ranges_W.r:1691–1699` (call `DISC_update_theta_synth_cpp_W`); `DISC_kalman_synth.cpp:916–1024` (forecast filtering); `DISC_kalman_synth.cpp:1031–1212` + `DISC_kalman_synth.cpp:1140–1210` (forecast smoothing loops) | Forecast period assimilates *only* forecast-product observations (baseline obs absent): `FF_list_ens` uses columns 2..(J+1) of the stacked `FF` (forecaster columns), each of which still contains the baseline block contribution. Additive evolution covariance is provided via `W_list_ens` (passed in from R). |
| T4 | `main.tex:162–171` (`eq:aug_y`–`eq:aug_s`) | `DISC_Optimal_Synth_Ranges_W.r:114–120` (`log.g`, `p.fn`, `A.fn`, `B.fn`, `C.fn`); `DISC_Optimal_Synth_Ranges_W.r:1244–1310` (`update_gamma_sigma` uses `a,b,c` and `abs(gam)`); `DISC_Optimal_Synth_Ranges_W.r:1180–1226` (`update_sts`, `update_uts`) | R implements the augmented-Gaussian building blocks via expectations (`E.*`) and uses them to construct VB pseudo-data for Kalman updates. |
| T5 | `main.tex:175–181` (defs of \(g,p,A,B,C\); admissible \((L,U)\)) | `DISC_Optimal_Synth_Ranges_W.r:114–120` (same formulas); `DISC_Optimal_Synth_Ranges_W.r:115–116` (`L.fn`,`U.fn`) | Code computes \((L,U)\) by root-finding `g(γ)=1-p0` on \((-\infty,0)\) and `g(γ)=p0` on \((0,\infty)\), consistent with feasibility of \(p(p_0,\gamma)\in(0,1)\). |
| T6 | `main.tex:216–235` (`eq:cond_v`) | `DISC_Optimal_Synth_Ranges_W.r:1208–1226` (`update_uts`); `sampling_exal.cpp:36–110` (`sample_gig_devroye`, `sample_gig_devroye_vector`); `DISC_Optimal_Synth_Ranges_W.r:2117` and `:2203` (calls) | `update_uts` forms \(\lambda=0.5\), \(\psi = E[A^2/(σB)] + 2E[1/σ]\) and \(\chi\) from the quadratic expansion; sampling uses `sample_gig_devroye_vector(n, lambda, psi, chi_vec)` (so `a=psi`, `b_vec=chi`). |
| T7 | `main.tex:237–253` (`eq:cond_s_V`, `eq:cond_s_m`) | `DISC_Optimal_Synth_Ranges_W.r:1180–1190` (`update_sts`); `sampling_truncnorm.cpp` (`sample_truncnorm_icdf`); `DISC_Optimal_Synth_Ranges_W.r:2119` and `:2205` (calls) | `update_sts` computes truncated-normal variance `sts.sig2` and mean `sts.mu`; sampling uses `sample_truncnorm_icdf(n_samp, TT, sts_mu, sts_sig2)` (icdf; truncates at 0). A backward-compatible alias `sample_truncnorm` is also provided. |
| T8 | `main.tex:255–298` (`eq:cond_state_path` + “Multiple observations per time”) | `DISC_Optimal_Synth_Ranges_W.r:1640–1646` (construct `FFF`,`QQQ`); `DISC_kalman_synth.cpp:861–905` (historical filtering with offset+diag covariance); `DISC_kalman_synth.cpp:949–972` (forecast filtering with stacking/expansion) | Code represents VB/Gibbs pseudo-observations via an *offset trick*: assimilate `y` with mean `H^T x + ex_f` so innovation is `y - (H^T x + ex_f) = (y - ex_f) - H^T x`, equivalent to using pseudo-observation \(\bar y\). Measurement covariance is `ex_q` (diag). |
| T9 | `main.tex:751–786` (`eq:kf_pred_mean`–`eq:kf_filt_cov`) | `DISC_kalman_synth.cpp:851–905` (`DISC_update_theta_synth_cpp_W`) | Standard Kalman predict/update is implemented in covariance form: `a = G*m`, `R = P + W`, `f = H^T a + ex_f`, `q = H^T R H + ex_q`, `m = a + R H q^{-1}(y-f)`, `C = R - R H q^{-1} H^T R`. |
| T10 | `main.tex:806–820` (`eq:ffbs_J`) + `main.tex:855–861` (`eq:rts_mean`, `eq:rts_cov`) | `DISC_kalman_synth.cpp:1277–1321` (RTS for historical block); `DISC_kalman_synth.cpp:1041–1212` (RTS-like smoothing for forecast blocks) | Backward pass uses `sB = C_t G_{t+1}^T R_{t+1}^{-1}` and the standard RTS mean/covariance updates, applied both to historical and forecast segments (forecast uses variable-dimension `GG_list_ens`). |
| T11 | `main.tex:604–621` (`eq:vb_info_wb`), `main.tex:836–846` (`eq:vb_pseudodata_scalar`) | `DISC_Optimal_Synth_Ranges_W.r:1640–1646` (`FFF`,`QQQ`); `DISC_Optimal_Synth_Ranges_W.r:1691–1699` (pass into Kalman) | In LaTeX: \(\bar R = 1/w\), \(\bar y=b/w\). In R/C++: set `ex_q = 1/w` and `ex_f = y-\bar y` so innovations become \(\bar y - H^T x\). |
| T12 | `main.tex:987–1051` (`eq:transform_u_xi`, `eq:jacobian_u_xi`, `eq:delta_method_2d`) | `DISC_Optimal_Synth_Ranges_W.r` (`update_gamma_sigma` transform+Jacobian); `DISC_Optimal_Synth_Ranges_W.r` (Laplace mode + Hessian) | Code now uses the logistic \(\gamma=L+(U-L)\pi(\xi)\) mapping and the matching log-Jacobian term, consistent with LaTeX (see `discrepancy_resolution_notes.md`). |
| T13 | `main.tex:183–191` (priors) | `DISC_Optimal_Synth_Ranges_W.r:1230–1237` (`PriorGammaDens`); `DISC_Optimal_Synth_Ranges_W.r:1261–1263` (prior terms in `update_gamma_sigma`) | Gamma prior uses truncated Student-t via `crch::dtt`; sigma prior uses inverse-gamma kernel \(\propto\sigma^{-(a+1)}\exp(-b/\sigma)\), consistent with LaTeX’s IG parameterization (up to constants). |
